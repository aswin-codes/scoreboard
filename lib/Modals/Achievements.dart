class Achievement {
  String type;
  String title;
  String? subtitle;
  int points;
  Achievement(
      {required this.type,
      required this.title,
      required this.subtitle,
      required this.points});
}
